# Production API endpoint (required for release builds)
EXPO_PUBLIC_API_BASE_URL=https://api.example.com/api/v1

# Dev only: your machine's LAN IP, used by iOS devices to reach a local backend.
# Find it with `ipconfig getifaddr en0` on macOS.
EXPO_PUBLIC_DEV_LAN_IP=192.168.1.10
